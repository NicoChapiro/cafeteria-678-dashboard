generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Branch {
  id        String   @id @default(cuid())
  name      String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  itemCosts     ItemCostVersion[]
  productPrices ProductPriceVersion[]
  productCosts  ProductCostVersion[]
}

model Item {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  costVersions ItemCostVersion[]
}

model Product {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  priceVersions ProductPriceVersion[]
  costVersions  ProductCostVersion[]
}

model ItemCostVersion {
  id                String   @id @default(cuid())
  itemId            String
  branchId          String
  packQtyInBase     Decimal
  packCostGrossClp  Int
  yieldRateOverride Decimal?
  validFrom         DateTime
  validTo           DateTime?
  createdAt         DateTime @default(now())

  item   Item   @relation(fields: [itemId], references: [id])
  branch Branch @relation(fields: [branchId], references: [id])

  @@index([itemId, branchId, validFrom])
}

model ProductPriceVersion {
  id            String   @id @default(cuid())
  productId     String
  branchId      String
  priceGrossClp Int
  validFrom     DateTime
  validTo       DateTime?
  createdAt     DateTime @default(now())

  product Product @relation(fields: [productId], references: [id])
  branch  Branch  @relation(fields: [branchId], references: [id])

  @@index([productId, branchId, validFrom])
}

model ProductCostVersion {
  id           String   @id @default(cuid())
  productId    String
  branchId     String
  costGrossClp Int
  validFrom    DateTime
  validTo      DateTime?
  createdAt    DateTime @default(now())

  product Product @relation(fields: [productId], references: [id])
  branch  Branch  @relation(fields: [branchId], references: [id])

  @@index([productId, branchId, validFrom])
}

model AuditLog {
  id         String   @id @default(cuid())
  entityType String
  entityId   String
  action     String
  diffJson   String
  userId     String?
  createdAt  DateTime @default(now())
}
